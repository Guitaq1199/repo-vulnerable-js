name: DevSecOps Pipeline - SAST + Docker Security

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  pipeline:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install dependencies
        run: npm install

      # ── SAST: CodeQL ──────────────────────────────────────────────────────────
      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3       # v3, not v4
        with:
          languages: javascript

      - name: Run CodeQL Analysis
        uses: github/codeql-action/analyze@v3    # v3, not v4

      # ── SBOM generation ───────────────────────────────────────────────────────
      - name: Generate SBOM with CycloneDX
        run: npm sbom --sbom-format cyclonedx > sbom.json

      - name: Upload SBOM as artifact
        uses: actions/upload-artifact@v4
        with:
          name: sbom
          path: sbom.json

      # ── Vulnerability scan with Grype ─────────────────────────────────────────
      - name: Scan SBOM with Grype (table — console output)
        uses: anchore/grype-action@v0.10.0      
        with:
          path: sbom.json                     
          fail-build: true
          severity-cutoff: medium
          output-format: table
          only-fixed: false

      # # Upload findings to GitHub Security tab (requires SARIF format)
      # - name: Scan SBOM with Grype (SARIF — Security tab)
      #   uses: anchore/grype-action@v0.10.0
      #   with:
      #     path: sbom.json
      #     fail-build: false                      # let the upload step handle failure
      #     severity-cutoff: medium
      #     output-format: sarif
      #     output-file: grype-results.sarif

      # - name: Upload Grype SARIF to GitHub Security
      #   uses: github/codeql-action/upload-sarif@v3
      #   with:
      #     sarif_file: grype-results.sarif